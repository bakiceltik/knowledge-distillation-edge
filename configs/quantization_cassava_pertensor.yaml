experiment_name: student_quantized_cassava_pertensor

data_dir: data/raw/cassava
output_dir: outputs/student_quantized_cassava_pertensor
subset_prefix: null

checkpoint: outputs/mobilenetv3_distilled_resnet50_cassava/best_model.pth

model_name: mobilenet_v3_small
num_classes: 5

image_size: 224
batch_size: 32
val_ratio: 0.15
test_ratio: 0.15
seed: 42
num_workers: 2

quantization:
  # Identical to post_training_static except for per-tensor weight quantization.
  # Isolates weight granularity as a factor in the int8 accuracy collapse.
  mode: post_training_static
  backend: fbgemm
  calibration_batches: 32
  per_channel: false
