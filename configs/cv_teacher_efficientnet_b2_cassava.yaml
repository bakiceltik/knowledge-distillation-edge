# 5-fold CV of the EfficientNet-B2 teacher trained supervised (baseline mode).
experiment_name: cv_teacher_efficientnet_b2_cassava

data_dir: data/raw/cassava
output_dir: outputs/cv_teacher_efficientnet_b2_cassava
subset_prefix: null

n_folds: 5

model_name: efficientnet_b2
pretrained: true

image_size: 224
batch_size: 64
epochs: 10
learning_rate: 0.0003
weight_decay: 0.0001

val_ratio: 0.15
seed: 42
num_workers: 2
