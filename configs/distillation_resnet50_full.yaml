# Reconstructed from outputs/mobilenetv3_distilled_resnet50_full/metrics.json;
# the original config was not committed. Distils the 38-class PlantVillage
# ResNet50 teacher into MobileNetV3-Small. See teacher_resnet50_full.yaml for
# the data_dir caveat.
experiment_name: mobilenetv3_distilled_resnet50_full

data_dir: data/raw/PlantVillage/train
output_dir: outputs/mobilenetv3_distilled_resnet50_full
subset_prefix: null

pretrained: true

teacher_model_name: resnet50
teacher_checkpoint: outputs/teacher_resnet50_full/best_model.pth
student_model_name: mobilenet_v3_small

image_size: 224
batch_size: 32
epochs: 15
learning_rate: 0.0005
weight_decay: 0.0001

temperature: 4.0
alpha: 0.5

val_ratio: 0.15
test_ratio: 0.15
seed: 42
num_workers: 2
